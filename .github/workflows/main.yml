name: Generar Video Viral
on:
  repository_dispatch:
    types: [generar_video_viral]
jobs:
  construir:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Instalar Dependencias
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg
          pip install google-generativeai edge-tts requests
      - name: 🚀 EJECUTAR MOTOR LEYENDA
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: python main.py --tema "${{ github.event.client_payload.tema }}" --id "${{ github.event.client_payload.id }}"
      - name: 📦 GUARDAR EN EL HISTORIAL
        run: |
          git config --global user.name "Motor Fenix"
          git config --global user.email "bot@fenix.com"
          git add static/*.mp4 -f
          git commit -m "Video: ${{ github.event.client_payload.tema }}" || echo "Sin cambios"
          git push origin main --force
